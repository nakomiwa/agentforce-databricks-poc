# Named Credential / External Credential について

PAT（個人用アクセストークン）は「シークレット」であり、メタデータファイルに含めて
デプロイすることができない。したがって **Setup 画面から手作業で作成する**のが確実。
手順は プロジェクト直下の README.md の「手順 B-1」を参照。

メタデータで管理したくなった場合のみ、以下を force-app 配下にコピーして使う。
その場合も、シークレット値は作成後に Setup 画面で入力する必要がある。
